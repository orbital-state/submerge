Reflection allows syncing any newest state of codebase with the latest state of the prompt. This is useful for keeping the codebase up to date with the latest changes in the prompt, especially when the prompt is being actively developed or modified.

Specifically, reflection can be used to:
- Pre-generate or regenerate `.div/contracts/` files for all modules/classes/functions in the codebase.